# 🔧 修復 404 錯誤指南

## ✅ 已修復的配置

### 1. 簡化 vercel.json
已更新為最簡單可靠的配置：
- 明確指定 `buildCommand` 和 `outputDirectory`
- 設置 `framework: "vite"` 讓 Vercel 自動識別
- 所有路由重定向到 `index.html`（SPA 必需）

### 2. 構建測試
✅ 構建成功，所有文件都在 `dist/` 目錄

## 🚀 重新部署步驟

### 方法 1: 使用 Vercel Dashboard（推薦）

1. **前往 Vercel Dashboard**
   - 打開 https://vercel.com/dashboard
   - 找到你的專案

2. **檢查專案設定**
   - 點擊專案 → Settings → General
   - 確認：
     - **Framework Preset**: `Vite`
     - **Build Command**: `npm run build`
     - **Output Directory**: `dist`
     - **Install Command**: `npm install`（預設）

3. **重新部署**
   - 點擊 "Deployments" 標籤
   - 找到最新的部署
   - 點擊 "..." → "Redeploy"
   - 或直接推送新的 commit 到 Git

### 方法 2: 使用 Vercel CLI

```bash
# 確保在專案根目錄
cd "C:\Users\aley0\OneDrive\桌面\langlive-roulette-pwa"

# 重新部署
vercel --prod
```

### 方法 3: 推送 Git Commit

```bash
git add .
git commit -m "Fix 404: Update vercel.json configuration"
git push
```

Vercel 會自動觸發新的部署。

## ✅ 部署後檢查

### 1. 檢查部署狀態
- 前往 Vercel Dashboard
- 確認部署狀態為 "Ready"（綠色）
- 沒有錯誤訊息

### 2. 測試網址
- 打開：`https://your-project.vercel.app`
- **不應該**看到 404 錯誤
- 應該看到輪盤紀錄界面

### 3. 測試路由
- 直接訪問根路徑：`https://your-project.vercel.app/`
- 應該正常顯示

## 🐛 如果仍然出現 404

### 檢查清單

1. **確認 vercel.json 存在**
   ```bash
   # 檢查文件是否存在
   cat vercel.json
   ```

2. **確認構建輸出**
   ```bash
   # 檢查 dist 目錄
   ls dist/
   # 應該看到 index.html
   ```

3. **檢查 Vercel 設定**
   - 前往 Vercel Dashboard
   - Settings → General
   - 確認 Output Directory 是 `dist`（不是 `build` 或 `public`）

4. **清除 Vercel 快取**
   - Settings → General
   - 找到 "Clear Build Cache"
   - 點擊清除後重新部署

## 📝 重要提醒

1. **必須使用 HTTPS**
   - Vercel 自動提供 HTTPS
   - 不要使用 HTTP

2. **vercel.json 位置**
   - 必須在專案**根目錄**
   - 不是 `src/` 或 `dist/` 目錄

3. **構建輸出**
   - 確保 `dist/index.html` 存在
   - 確保所有資源文件在 `dist/assets/` 目錄

## 🔍 常見 404 原因

1. **輸出目錄錯誤**
   - 解決：確認 `outputDirectory: "dist"`

2. **路由配置錯誤**
   - 解決：確認 `rewrites` 規則正確

3. **構建失敗**
   - 解決：檢查構建日誌，確保沒有錯誤

4. **快取問題**
   - 解決：清除 Vercel 快取後重新部署

## 📞 如果問題持續

請提供：
1. Vercel Dashboard 的部署日誌截圖
2. 瀏覽器控制台的錯誤訊息
3. 完整的錯誤 ID（如：`hkg1::k7mll-1768973049371-3f00e83fc991`）

這樣可以更準確地診斷問題。
